'use client';
import Link from 'next/link';
import { ArrowLeft, BarChart3, Clock3, MapPin, Radio, ShieldCheck } from 'lucide-react';
import { useParams } from 'next/navigation';
import { Card, TeamBadge } from '@/components/ui';
import { useApi } from '@/lib/api';
export default function MatchPage(){const {id}=useParams<{id:string}>();const {data:match,error,loading}=useApi<any>(`/public/matches/${id}`);if(loading)return <main className="page-shell">Loading match…</main>;if(error||!match)return <main className="page-shell"><div className="admin-error">{error||'Match not found'}</div></main>;const facts=match.facts??[];const homePlayers=match.participants.filter((item:any)=>item.teamId===match.homeTeamId);return <main className="page-shell"><Link href="/scores" className="back"><ArrowLeft/> Match centre</Link><div className="match-header"><div className="match-meta"><span><Radio/> {match.status}</span><b>{match.season.competition.name} · {match.round??match.season.label}</b><small><MapPin/> {match.venue?.name??match.venueName??'Venue TBC'}</small></div><div className="match-score"><div><TeamBadge short={match.homeTeam.shortName} color={match.homeTeam.primaryColor}/><h2>{match.homeTeam.name}</h2><small>HOME</small></div><div><strong>{match.homeScore} <i>—</i> {match.awayScore}</strong><span>{match.status}</span></div><div><TeamBadge short={match.awayTeam.shortName} color={match.awayTeam.primaryColor}/><h2>{match.awayTeam.name}</h2><small>AWAY</small></div></div><div className="verified"><ShieldCheck/> Published events are backed by consensus observations</div></div><nav className="match-tabs"><a href="#timeline">Timeline</a><a href="#lineups">Lineups</a><Link href={`/collect/matches/${id}`}>Collect data</Link></nav><div className="match-layout"><div><h3 id="timeline">Match timeline</h3><Card className="timeline">{facts.length?facts.map((fact:any)=><div key={fact.id}><time>{fact.matchSecond!=null?`${Math.floor(fact.matchSecond/60)}'`:'—'}</time><span className="event"><Clock3/></span><p><b>{fact.factType.replaceAll('_',' ')}</b><small>{fact.subjectId??'Team event'}</small></p><strong>{Math.round(fact.confidence*100)}%</strong></div>):<div className="empty-state">No verified events published yet.</div>}</Card></div><aside><h3 id="lineups">Registered lineup</h3><Card className="lineup">{homePlayers.map((item:any)=><div className="lineup-team" key={item.playerId}><TeamBadge small short={String(item.shirtNumber??'–')} color={match.homeTeam.primaryColor}/><b>{item.player.knownAs??item.player.legalName}</b><span>{item.player.position}</span></div>)}</Card><div className="data-note"><BarChart3/><div><b>Verification model</b><p>Independent observations are compared before a match fact is published.</p></div></div></aside></div></main>}
