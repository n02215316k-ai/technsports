'use client';
import Link from 'next/link';
import { ArrowLeft, ArrowUpRight, MapPin } from 'lucide-react';
import { useParams } from 'next/navigation';
import { Card, TeamBadge } from '@/components/ui';
import { formatDate, initials, useApi } from '@/lib/api';
export default function ClubProfile(){const {slug}=useParams<{slug:string}>();const {data:team,error,loading}=useApi<any>(`/public/teams/${slug}`);if(loading)return <main className="page-shell">Loading club…</main>;if(error||!team)return <main className="page-shell"><div className="admin-error">{error||'Club not found'}</div></main>;const matches=[...team.homeMatches.map((match:any)=>({...match,homeTeam:team,awayTeam:match.awayTeam})),...team.awayMatches.map((match:any)=>({...match,homeTeam:match.homeTeam,awayTeam:team}))].sort((a,b)=>new Date(b.kickoffAt).getTime()-new Date(a.kickoffAt).getTime());const finished=matches.filter(match=>match.status==='FINISHED');const wins=finished.filter(match=>(match.homeTeamId===team.id?match.homeScore>match.awayScore:match.awayScore>match.homeScore)).length;const draws=finished.filter(match=>match.homeScore===match.awayScore).length;const goals=finished.reduce((sum,match)=>sum+(match.homeTeamId===team.id?match.homeScore:match.awayScore),0);const players=[...new Map(team.registrations.map((registration:any)=>[registration.player.id,registration.player])).values()] as any[];const transfers=[...team.transfersTo,...team.transfersFrom].sort((a:any,b:any)=>new Date(b.announcedAt).getTime()-new Date(a.announcedAt).getTime());return <main className="page-shell"><Link href="/table" className="back"><ArrowLeft/> Back to clubs</Link><section className="profile-hero club-profile"><div>{team.crestUrl?<img className="club-crest" src={team.crestUrl} alt={`${team.name} logo`}/>:<TeamBadge short={team.shortName} color={team.primaryColor}/>}<div><span>CLUB PROFILE</span><h1>{team.name}</h1><p><MapPin/> {team.city??'Zimbabwe'}{team.foundedYear?` · Founded ${team.foundedYear}`:''}</p></div></div></section><nav className="profile-tabs"><a href="#squad">Squad</a><a href="#matches">Matches</a><a href="#records">Records</a><a href="#transfers">Transfers</a></nav><section id="squad" className="anchor-section"><div className="records-heading"><div><span className="eyebrow">REGISTERED PLAYERS</span><h2>Squad</h2></div></div><div className="club-squad">{players.map(player=><Link href={`/players/${player.slug}`} key={player.id}><i>{initials(player.knownAs??player.legalName)}</i><span><b>{player.knownAs??player.legalName}</b><small>{player.position}</small></span></Link>)}</div></section><section id="matches" className="anchor-section"><div className="records-heading"><div><span className="eyebrow">FIXTURES</span><h2>Recent & upcoming</h2></div><Link className="verified-chip" href="/fixtures">All fixtures <ArrowUpRight/></Link></div><div className="player-matches">{matches.slice(0,10).map(match=><Link href={`/matches/${match.id}`} key={match.id}><time>{formatDate(match.kickoffAt)}</time><span>{match.homeTeam.name} <b>{match.status==='SCHEDULED'?'—':`${match.homeScore}—${match.awayScore}`}</b> {match.awayTeam.name}</span><strong>{match.status}</strong><em>{match.round??''}</em></Link>)}</div></section><section id="records" className="anchor-section"><div className="records-heading"><div><span className="eyebrow">DATABASE TOTALS</span><h2>Recorded results</h2></div></div><div className="record-grid"><Card><span>Matches</span><strong>{finished.length}</strong></Card><Card><span>Wins</span><strong>{wins}</strong></Card><Card><span>Draws</span><strong>{draws}</strong></Card><Card><span>Losses</span><strong>{finished.length-wins-draws}</strong></Card><Card><span>Goals</span><strong>{goals}</strong></Card></div></section><section id="transfers" className="anchor-section"><div className="records-heading"><div><span className="eyebrow">SQUAD MOVEMENT</span><h2>Transfers</h2></div></div><div className="transfer-history">{transfers.map((move:any)=><div key={move.id}><time>{formatDate(move.announcedAt)}</time><span>{move.player.knownAs??move.player.legalName}</span><b>{move.type.replace('_',' ')}</b><small>{move.verified?'Verified':'Pending'}</small></div>)}</div></section></main>}
